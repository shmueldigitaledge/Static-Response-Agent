# Hebrew Chat Widget Configuration

# External Database API Base URL (set to 'fake' for testing)
DB_API_BASE=fake

# Optional API Key for external DB service
DB_API_KEY=

# Deepgram API Key for Speech-to-Text (optional - uses mock STT if not provided)
# DEEPGRAM_API_KEY=your_deepgram_api_key_here

# Optional: Specify allowed origin for CORS in production
# ORIGIN_URL=https://yourdomain.com

# Server Configuration
PORT=3000
NODE_ENV=development